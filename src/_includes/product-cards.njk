<div>
  {%- for batch in product | batch(2) -%}
    <div class="grid">
      {%- for product in batch -%}
        <div>
          <article>
            <i class="{{ product.icon }} card-icons"></i>
            <br/>
            <p class="product-title">{{ product.title }}</p>
            <p>{{ product.description }}</p>
          </article>
        </div>
      {%- endfor %}
    </div>
  {%- endfor %}
</div>
