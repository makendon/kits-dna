<div>
  {%- for batch in collections.posts | reverse | batch(3) -%}
    <div class="grid">
      {%- for post in batch -%}
        <div>
          <article>
            <header>
              <small><code>{{ post.date | dateFormat }}</code></small>
            </header>
            <h4><a href="{{ post.url }}">{{ post.data.title }}</a></h4>
            <p>{{ post.data.description }}</p>
          </article>
        </div>
      {%- endfor -%}
    </div>
  {%- endfor -%}
</div>
